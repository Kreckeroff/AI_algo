# C9 — multi-symbol (§7F)

- Train intervention policy on **SBER + GAZP + LKOH** (1d)
- Reuses C8 intervention variants; dataset ×3 with `symbol` features
- Leave-one-symbol-out (LOSO) generalization check
- Promote only **cross-symbol** wins (≥2 tickers) as `29p-*`

Session: `artifacts/agent_loop/sessions/2026-08-21-c9-multi-symbol/`
