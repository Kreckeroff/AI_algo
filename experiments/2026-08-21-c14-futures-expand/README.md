# C14 — futures expand (§7F + §7H control)

Add **CNYRUBF, GLDRUBF, IMOEXF** (futures — no dividend gap) to all-TF training.
New intervention: `change_period_05x`. Feature `is_future` / `asset_class`.
Promote `34p-*` when ≥6 symbols win on 1d.

Equities LS still provisional until P3.7 cash-adjust.
